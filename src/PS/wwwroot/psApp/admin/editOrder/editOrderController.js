"use strict";

angular.module("psApp").controller("editOrderController", [
        "$scope", "psDataServices", "$state", "$localStorage", function($scope, psDataServices, $state, $localStorage) {

        }
    ]
);